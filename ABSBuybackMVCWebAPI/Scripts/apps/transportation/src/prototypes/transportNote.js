import {TransportNoteFunctions as transNoteFuncs} from "../functions/transportNote"

export let TransportNote = {
    instanceMembers: {},
    protoMembers: {},
    closures: [
      function() {}
    ]
};